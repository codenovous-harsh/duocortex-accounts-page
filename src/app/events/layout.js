export const metadata = {
  title: "Duocortex Events - Upcoming Events & Registrations",
  description: "Register for exciting Duocortex events and expand your network",
};

export default function EventsLayout({ children }) {
  return children;
}